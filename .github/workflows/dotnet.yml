# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET and Python Tests

on:
  push:
    branches: 
      - "C#-Cargohub"
  pull_request:
    branches: 
      - "C#-Cargohub"
  

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    
    - name: Restore dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    # # Run .NET tests
    # - name: Test
    #   run: dotnet test --no-build --verbosity normal
    
    # - name: Set up Python
    #   uses: actions/setup-python@v4
    #   with:
    #     python-version: '3.12'
    
    # - name: Install pytest
    #   run: |
    #     python -m pip install --upgrade pip
    #     pip install pytest
    
    # - name: Run pytest tests
    # # -v for more detailed output (easier debugging)
    #   run: pytest ./Integrationtests/endpoints_tests.py -v

    - name: Update SERVER
      run: |

        #Kill previous dotnet programs
        pkill -f 'dotnet run' || true
        
        # Clone the repo into a persistent directory
        mkdir -p ~/persistent-dir
        git clone https://github.com/HamsterChief/Cargohub-Inf2.git ~/persistent-dir/Cargohub-Inf2
        
        # Change to the project directory
        cd ~/persistent-dir/Cargohub-Inf2/MyEFCoreProject
        
        # Start the application in the background with nohup
        nohup dotnet run --urls "http://0.0.0.0:5072" > ~/persistent-dir/output.log 2>&1 &
        
        # Check running processes and ports
        ps aux | grep 'dotnet'
        lsof -i :5072