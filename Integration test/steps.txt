-----------------------------------------------------

1. Install packages:

> pip install pytest pytest-ordering pytest-cov coverage requests

-----------------------------------------------------

2. Create new VSCode project and with endpoints_tests.py

-----------------------------------------------------

3. Run tests:

run this instead:
> python -m pytest --cov=. --cov-report=html endpoints_tests.py


not this one:
> pytest --cov=. --cov-report=html endpoints_tests.py

-----------------------------------------------------